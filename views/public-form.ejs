<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %>
    </title>
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        :root {
            --primary-color: #4361ee;
            --secondary-color: #3f37c9;
            --accent-color: #4cc9f0;
            --bg-color: #f8f9fa;
            --bg-gradient: linear-gradient(135deg, #f8f9fa 0%, #e9ecef 100%);
            --card-bg: rgba(255, 255, 255, 0.9);
            --text-main: #2b2d42;
            --text-muted: #6c757d;
            --border-color: #dee2e6;
            --input-bg: #ffffff;
            --card-shadow: 0 15px 35px rgba(0, 0, 0, 0.05);
            --transition: all 0.3s ease;
        }

        [data-theme='dark'] {
            --bg-color: #0b0e14;
            --bg-gradient: linear-gradient(135deg, #0b0e14 0%, #1a1d23 100%);
            --card-bg: rgba(26, 29, 35, 0.9);
            --text-main: #f8f9fa;
            --text-muted: #adb5bd;
            --border-color: #343a40;
            --input-bg: #111418;
            --card-shadow: 0 15px 35px rgba(0, 0, 0, 0.3);
        }

        body {
            font-family: 'Outfit', sans-serif;
            background: var(--bg-gradient);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 2rem 0;
            color: var(--text-main);
            transition: var(--transition);
        }

        .form-container {
            width: 100%;
            max-width: 650px;
            padding: 1rem;
        }

        .form-card {
            background: var(--card-bg);
            backdrop-filter: blur(10px);
            border: 1px solid var(--border-color);
            border-radius: 24px;
            box-shadow: var(--card-shadow);
            overflow: hidden;
            animation: slideUp 0.6s cubic-bezier(0.16, 1, 0.3, 1);
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }

            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        .card-header-premium {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            padding: 2.5rem 2rem;
            text-align: center;
            color: white;
            position: relative;
            overflow: hidden;
        }

        .card-header-premium::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: radial-gradient(circle, rgba(255, 255, 255, 0.1) 0%, transparent 70%);
            animation: rotate 20s linear infinite;
        }

        @keyframes rotate {
            from {
                transform: rotate(0deg);
            }

            to {
                transform: rotate(360deg);
            }
        }

        .card-header-premium h2 {
            font-weight: 700;
            margin-bottom: 0.5rem;
            position: relative;
            z-index: 1;
        }

        .card-header-premium p {
            opacity: 0.9;
            font-weight: 300;
            margin-bottom: 0;
            position: relative;
            z-index: 1;
        }

        .card-body-premium {
            padding: 2.5rem;
        }

        .form-floating>.form-control {
            border-radius: 12px;
            border: 1.5px solid var(--border-color);
            background-color: var(--input-bg);
            color: var(--text-main);
            padding: 1rem 1.25rem;
            height: calc(3.5rem + 2px);
            transition: var(--transition);
        }

        .form-floating>textarea.form-control {
            height: 120px;
        }

        .form-control:focus {
            border-color: var(--primary-color);
            background-color: var(--input-bg);
            color: var(--text-main);
            box-shadow: 0 0 0 0.25rem rgba(67, 97, 238, 0.15);
        }

        .form-floating>label {
            padding: 1rem 1.25rem;
            color: var(--text-muted);
        }

        .btn-submit {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border: none;
            border-radius: 12px;
            padding: 1rem 2rem;
            font-weight: 600;
            color: white !important;
            width: 100%;
            margin-top: 1rem;
            transition: var(--transition);
            box-shadow: 0 4px 15px rgba(67, 97, 238, 0.3);
        }

        .btn-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(67, 97, 238, 0.4);
            opacity: 0.9;
        }

        .success-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--card-bg);
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            z-index: 100;
            text-align: center;
            padding: 2rem;
            animation: fadeIn 0.4s ease-out;
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }

            to {
                opacity: 1;
            }
        }

        .success-icon {
            width: 80px;
            height: 80px;
            background: rgba(5, 150, 105, 0.1);
            color: #10b981;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            margin-bottom: 1.5rem;
            animation: scaleIn 0.5s cubic-bezier(0.16, 1, 0.3, 1);
        }

        @keyframes scaleIn {
            from {
                transform: scale(0.5);
                opacity: 0;
            }

            to {
                transform: scale(1);
                opacity: 1;
            }
        }

        .input-group-text {
            background: transparent;
            border-radius: 12px;
            border: 1.5px solid var(--border-color);
            color: var(--text-muted);
        }

        .input-group .form-control {
            border-left: none;
        }

        .input-group .input-group-text {
            border-right: none;
        }

        .section-title {
            font-size: 0.9rem;
            text-transform: uppercase;
            letter-spacing: 1px;
            font-weight: 600;
            color: var(--text-muted);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
        }

        .section-title::after {
            content: '';
            height: 1px;
            background: var(--border-color);
            flex-grow: 1;
            margin-left: 1rem;
        }

        /* Dark Mode Specific Adjustments */
        [data-theme='dark'] .form-control::placeholder {
            color: #495057;
        }

        [data-theme='dark'] .alert-danger {
            background-color: rgba(220, 53, 69, 0.1);
            color: #ff6b6b;
            border: 1px solid rgba(220, 53, 69, 0.2);
        }
    </style>
</head>

<body>


    <div class="form-container">
        <div class="form-card position-relative">

            <% if (success) { %>
                <div class="success-overlay">
                    <div class="success-icon">
                        <i class="fas fa-check"></i>
                    </div>
                    <h3 class="fw-bold text-dark">Submitted Successfully!</h3>
                    <p class="text-muted mb-4">Your internship application has been received. We will contact you soon.
                    </p>
                    <a href="/apply" class="btn btn-outline-primary rounded-pill px-4">
                        <i class="fas fa-plus me-2"></i> Submit Another Response
                    </a>
                </div>
                <% } %>

                    <div class="card-header-premium">
                        <img src="/logo.png" alt="Logo"
                            style="height: 40px; margin-bottom: 1rem; filter: brightness(0) invert(1);">
                        <h2>Internship Application</h2>
                        <p>Join our team and kickstart your career Journey</p>
                    </div>

                    <div class="card-body-premium">
                        <% if (error) { %>
                            <div class="alert alert-danger rounded-4 border-0 mb-4 py-3 d-flex align-items-center">
                                <i class="fas fa-exclamation-circle me-3 fs-4"></i>
                                <div>
                                    <%= error %>
                                </div>
                            </div>
                            <% } %>

                                <form action="/apply" method="POST">
                                    <div class="section-title">Personal Details</div>

                                    <div class="form-floating mb-4">
                                        <input type="text" class="form-control" id="studentName" name="studentName"
                                            placeholder="Full Name" required>
                                        <label for="studentName"><i class="fas fa-user me-2 text-primary"></i> Full
                                            Name</label>
                                    </div>

                                    <div class="row">
                                        <div class="col-md-6 mb-4">
                                            <div class="form-floating">
                                                <input type="tel" class="form-control" id="studentContactNo"
                                                    name="studentContactNo" placeholder="Contact Number" required
                                                    pattern="[0-9]{10}">
                                                <label for="studentContactNo"><i
                                                        class="fas fa-phone me-2 text-primary"></i> Contact No</label>
                                            </div>
                                        </div>
                                        <div class="col-md-6 mb-4">
                                            <div class="form-floating">
                                                <input type="text" class="form-control" id="collegeName"
                                                    name="collegeName" placeholder="College Name" required>
                                                <label for="collegeName"><i
                                                        class="fas fa-university me-2 text-primary"></i> College
                                                    Name</label>
                                            </div>
                                        </div>
                                    </div>

                                    <div class="form-floating mb-4">
                                        <textarea class="form-control" id="address" name="address"
                                            placeholder="Residential Address" required></textarea>
                                        <label for="address"><i class="fas fa-map-marker-alt me-2 text-primary"></i>
                                            Full Address</label>
                                    </div>

                                    <div class="section-title">Internship Preferences</div>

                                    <div class="form-floating mb-4">
                                        <input type="date" class="form-control" id="startDate" name="startDate"
                                            placeholder="Joining Date" required>
                                        <label for="startDate"><i class="fas fa-calendar-alt me-2 text-primary"></i>
                                            Preferred Joining Date</label>
                                    </div>

                                    <button type="submit" class="btn btn-submit">
                                        <i class="fas fa-paper-plane me-2"></i> Submit Application
                                    </button>
                                </form>
                    </div>
        </div>

        <div class="text-center mt-4 text-muted small">
            &copy; <%= new Date().getFullYear() %> Creative Web Infotech. All rights reserved.
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        // Set default date to today
        const startDateInput = document.getElementById('startDate');
        if (startDateInput) startDateInput.valueAsDate = new Date();
    </script>
</body>

</html>